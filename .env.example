# Copy to .env and adjust if needed
# To use Postgres later, set e.g.:
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/students

# Default uses local SQLite file in project root
DATABASE_URL=sqlite:///./students.db

